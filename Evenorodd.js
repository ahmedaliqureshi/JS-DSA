//Q) to check if the number is even or odd 

// 1st we are going to declare the variabel by using let 
const number=175; 
// 2nd then we are going to check if the number is odd or even 
// by using if else, with condition of if wherer 
if(number%2==0)
{
    console.log(`${number} is even`);
}
else
    console.log(`number is odd`);